# Hey there! 👋
Here I share a Tailwind CSS project with JavaScript and NodeJS
# Tailwind CSS project with JavaScript and NodeJS


## Requirements
*  Nodejs
    ```bash
    npm init -y
    ```
*  Tailwind, Post CSS autoprefixer
    ```bash
    npm install -D tailwindcss postcss autoprefixer
    ```
*  Tailwind configurations 
    ```bash
    npx tailwindcss init -p
    ```
  

To execute the app in this repository

1.  Navigate to the directory containing the  files in your terminal.
2.  Run the desired script in the terminal:
    ```bash
    npm run watch
    ```
Runs the app in development mode.

    
   
