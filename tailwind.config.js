/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{html,js}" // Add this line just in case, but your HTML is in the root
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}